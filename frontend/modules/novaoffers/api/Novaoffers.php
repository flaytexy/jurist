<?php

namespace frontend\modules\novaoffers\api;


use frontend\modules\novaoffers\models\Offers;
use frontend\modules\novaoffers\models\NovaoffersTranslation;
use Yii;
use yii\data\ActiveDataProvider;
use frontend\models\Tag;
use frontend\widgets\Fancybox;
use yii\widgets\LinkPager;
use frontend\modules\novaoffers\models\Novaoffers as NovaoffersModel;

/**
 * Page module API
 * @package frontend\modules\novaoffers\api
 *
 * @property ActiveDataProvider $_adp
 * @method static NovaoffersObject get(mixed $id_slug) Get page object by id or slug
 * @method static array items(array $options = []) Get list of page as NovaoffersObject objects
 * @method static mixed last(int $limit = 1) Get last page
 * @method static mixed clear() Clear items
 * @method static void plugin() Applies FancyBox widget on photos called by box() function
 * @method static string pages() returns pagination html generated by yii\widgets\LinkPager widget.
 * @method static \stdClass pagination() returns yii\data\Pagination object.
 */
class Novaoffers extends \common\components\API
{
    private $_adp;
    private $_last;
    private $_items;
    private $_item = [];

    public function api_items($options = [])
    {
        $this->_items = [];

        $with[] = 'seo';
        if (Yii::$app->getModule('admin')->activeModules['page']->settings['enableTags']) {
            $with[] = 'tags';
        }
        $query = NovaoffersModel::find()
            //->select([NovaoffersModel::tableName().'.*', NovaoffersTranslation::tableName().'.*'])
            ->joinWith('translation')
            ->joinWith('child')
            ->with($with)
            ->andWhere([NovaoffersModel::tableName() . '.type_id' => NovaoffersModel::TYPE_ID])
            ->andWhere([NovaoffersTranslation::tableName() . '.public_status' => NovaoffersTranslation::STATUS_ON])
            ->status(NovaoffersModel::STATUS_ON);

        if (!empty($options['where'])) {
            $query->andFilterWhere($options['where']);
        }

        if (!empty($options['tags'])) {
            $query
                ->innerJoinWith('tags', false)
                ->andWhere([Tag::tableName() . '.name' => (new NovaoffersModel)->filterTagValues($options['tags'])])
                ->addGroupBy('id');
        }

        if (!empty($options['orderBy'])) {
            if($options['orderBy']==='title'){
                $query->orderBy(NovaoffersTranslation::tableName().'.name');
            }else{
                $query->orderBy($options['orderBy']);
            }
        } else {
            $query->sortDate();
        }
//ex_print($query->createCommand()->rawSql);
        $this->_adp = new ActiveDataProvider([
            'query' => $query,
            'pagination' => !empty($options['pagination']) ? $options['pagination'] : []
        ]);

        /**
         * @var \frontend\modules\novaoffers\models\Novaoffers  $model
         */
        foreach ($this->_adp->models as $model) {
            $oneItem = new NovaoffersObject($model);
            $this->_items[] = $oneItem;
        }

        return $this->_items;
    }

    public function api_get($id_slug)
    {
        if (!isset($this->_item[$id_slug])) {
            $this->_item[$id_slug] = $this->findPage($id_slug);
        }
        return $this->_item[$id_slug];
    }

    public function api_last($limit = 1)
    {
        if ($limit === 1 && $this->_last) {
            return $this->_last;
        }

        $with = ['seo'];
        if (Yii::$app->getModule('admin')->activeModules['page']->settings['enableTags']) {
            $with[] = 'tags';
        }

        $result = [];
        foreach (NovaoffersModel::find()->with($with)->status(NovaoffersModel::STATUS_ON)->sortDate()->limit($limit)->all() as $item) {
            $result[] = new NovaoffersObject($item);
        }

        if ($limit > 1) {
            return $result;
        } else {
            $this->_last = count($result) ? $result[0] : null;
            return $this->_last;
        }
    }

    public function api_plugin($options = [])
    {
        Fancybox::widget([
            'selector' => '.easyii-box',
            'options' => $options
        ]);
    }

    public function api_pagination()
    {
        return $this->_adp ? $this->_adp->pagination : null;
    }

    public function api_pages()
    {
        $this->_adp->pagination->pageSizeParam = false;

        if ($this->_adp) {
            return LinkPager::widget(['pagination' => $this->_adp->pagination]);
        }
        return '';
    }

    private function findPage($id_slug)
    {
        $page = NovaoffersModel::find()
            ->joinWith('translation')
            ->joinWith('child')
            ->where(['or', NovaoffersModel::tableName().'.id=:id_slug', NovaoffersModel::tableName().'.slug=:id_slug'], [':id_slug' => $id_slug])
            ->andWhere([NovaoffersModel::tableName() . '.type_id' => NovaoffersModel::TYPE_ID])
            ->status(NovaoffersModel::STATUS_ON)
            ->one();

        if(!$page){

            $errorMessage = 'Страница без перевода!!!: '. $id_slug .'  .#36561';

            //if (!$this->mailApi($errorMessage)) {
                //header("HTTP/1.0 404 Not Found");
                //header("Location: " . Url::to(['/404/error/mailsend'], true));
                //exit;s
            //}

            $page = NovaoffersModel::find()
                //->joinWith('translation')
                ->where(['or', NovaoffersModel::tableName().'.id=:id_slug', NovaoffersModel::tableName().'.slug=:id_slug'], [':id_slug' => $id_slug])
                ->status(NovaoffersModel::STATUS_ON)
                ->one();
        }

        if ($page) {
            $page->updateCounters(['views' => 1]);
            $page = new NovaoffersObject($page);

            return $page;
        } else {
            return false;
        }
    }

    private function notFound($id_slug)
    {
        $page = new NovaoffersModel([
            'slug' => $id_slug
        ]);
        return new NovaoffersObject($page);
    }

    public function api_clear()
    {
        $this->_adp = false;
        $this->_last = false;
        $this->_items = false;
        $this->_item = [];
    }


}